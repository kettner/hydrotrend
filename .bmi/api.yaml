language: c
prefix: BMI_HYDROTREND
build:
  brew:
    formula: csdms/models/hydrotrend
    options: "--HEAD"
include:
  - "#include <bmi_hydrotrend.h>"
cflags:
  pkgconfig: hydrotrend
libs:
  pkgconfig: hydrotrend
